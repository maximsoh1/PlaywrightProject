# 🔧 GitHub Actions Workflows

## 📋 Активные Workflows

### ✅ `playwright-tests.yml` - Основной CI/CD Pipeline

**Запускается:**
- ✅ При push в ветки: `main`, `master`, `feature/Tests`
- ✅ При создании Pull Request
- ✅ Можно запустить вручную (workflow_dispatch)

**Что делает:**
1. Устанавливает .NET 9
2. Собирает проект
3. Устанавливает Playwright browsers
4. Запускает **все 35 тестов**
5. Загружает screenshots/videos при провалах

**Статус:** ✅ **АКТИВЕН**

---

## 🔒 Отключенные Workflows

Эти workflows отключены для упрощения. При необходимости их можно включить, удалив `.disabled` из имени файла.

### 🔵 `pr-tests.yml.disabled`
- **Назначение:** Специальные тесты для Pull Requests
- **Почему отключен:** Дублирует `playwright-tests.yml` (который тоже запускается на PR)
- **Включить:** `git mv pr-tests.yml.disabled pr-tests.yml`

### 🔵 `nightly-tests.yml.disabled`
- **Назначение:** Ночные тесты на всех браузерах (Chromium, Firefox, WebKit)
- **Запускается:** Каждую ночь в 2:00 UTC
- **Почему отключен:** Пока не нужен (проект в разработке)
- **Включить:** `git mv nightly-tests.yml.disabled nightly-tests.yml`

### 🔵 `accessibility-tests.yml.disabled`
- **Назначение:** Только accessibility тесты (WCAG проверки)
- **Запускается:** Вручную с параметрами
- **Почему отключен:** Можно запускать через основной workflow
- **Включить:** `git mv accessibility-tests.yml.disabled accessibility-tests.yml`

### 🔵 `manual-test-run.yml.disabled`
- **Назначение:** Ручной запуск с кастомными параметрами
- **Параметры:** фильтр тестов, браузер, headless mode, окружение
- **Почему отключен:** Основной workflow можно запустить вручную
- **Включить:** `git mv manual-test-run.yml.disabled manual-test-run.yml`

---

## 🎯 Когда включать дополнительные workflows?

### **Включить `pr-tests.yml` когда:**
- Нужны специальные smoke тесты для PR (быстрее основных)
- Хотите разные наборы тестов для push vs PR

### **Включить `nightly-tests.yml` когда:**
- Проект в production
- Нужно тестировать на нескольких браузерах каждую ночь
- Есть долгие E2E тесты

### **Включить `accessibility-tests.yml` когда:**
- Нужны детальные WCAG отчеты
- Работаете над accessibility compliance

### **Включить `manual-test-run.yml` когда:**
- Часто запускаете тесты с разными параметрами
- Нужна гибкость в выборе браузера/окружения

---

## 📊 Текущая структура

```
.github/workflows/
├── playwright-tests.yml          ✅ АКТИВЕН (основной)
├── pr-tests.yml.disabled         🔒 Отключен
├── nightly-tests.yml.disabled    🔒 Отключен
├── accessibility-tests.yml.disabled  🔒 Отключен
├── manual-test-run.yml.disabled  🔒 Отключен
└── README.md                     📚 Этот файл
```

---

## 🔗 Полезные ссылки

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Playwright CI Documentation](https://playwright.dev/docs/ci)
- [View Workflow Runs](https://github.com/maximsoh1/PlaywrightProject/actions)

---

**Дата создания:** 2024-02-18  
**Автор:** GitHub Copilot  
**Версия:** 1.0
